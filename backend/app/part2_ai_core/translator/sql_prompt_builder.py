from app.part1_data_security.integration.schema_inspector import get_database_schema_info


def build_sql_prompt(user_query: str, chat_history: list = None) -> str:
    """
    สร้าง Prompt สำหรับแปลงภาษาธรรมชาติเป็น SQL
    รวมทั้งคำถาม, โครงสร้างตาราง (Schema) และประวัติการสนทนา
    """
    schema = get_database_schema_info(relevant_query=user_query)

    history_lines = []
    if chat_history:
        for msg in chat_history[-5:]:
            if isinstance(msg, dict):
                role = msg.get("role", "user")
                text_content = msg.get("text") or msg.get("content") or ""
                if text_content:
                    history_lines.append(f"- {role}: {text_content}")
            elif hasattr(msg, "content"):
                role = getattr(msg, "role", "user")
                history_lines.append(f"- {role}: {msg.content}")

    history_str = ""
    if history_lines:
        history_str = "\nประวัติการสนทนาก่อนหน้า:\n" + "\n".join(history_lines)

    prompt = f"""คุณคือผู้เชี่ยวชาญด้าน SQLite Database ขั้นสูง
นี่คือโครงสร้างตารางและความสัมพันธ์ในฐานข้อมูลปัจจุบัน:
{schema}
{history_str}

คำถามจากผู้ใช้: "{user_query}"

ตัวอย่างรูปแบบคำถามและคำสั่ง SQL ที่ถูกต้อง (Few-Shot Examples):
- ตัวอย่าง 1 (ดูข้อมูลภาพรวม):
  คำถาม: "แสดงข้อมูลทั้งหมดในชุดข้อมูล" หรือ "แสดง 20 รายการแรก"
  SQL: SELECT * FROM "ชื่อตาราง" LIMIT 20;
- ตัวอย่าง 2 (จัดอันดับ / ค่าน้อย-มาก):
  คำถาม: "ค้นหา 5 อันดับแรกที่มีงบประมาณมากที่สุด"
  SQL: SELECT * FROM "ชื่อตาราง" ORDER BY "งบประมาณ" DESC LIMIT 5;
- ตัวอย่าง 3 (แจกแจง / จัดกลุ่มตามหมวดหมู่):
  คำถาม: "แจกแจงจำนวนโครงการตามแต่ละจังหวัด"
  SQL: SELECT "จังหวัด", COUNT(*) AS "จำนวนโครงการ" FROM "ชื่อตาราง" GROUP BY "จังหวัด" ORDER BY "จำนวนโครงการ" DESC LIMIT 10;
- ตัวอย่าง 4 (สรุปสถิติผลรวม / ค่าเฉลี่ย):
  คำถาม: "สรุปยอดงบประมาณรวมและงบประมาณเฉลี่ยทั้งหมด"
  SQL: SELECT COUNT(*) AS "จำนวนรายการ", SUM("งบประมาณ") AS "งบประมาณรวม", AVG("งบประมาณ") AS "งบประมาณเฉลี่ย" FROM "ชื่อตาราง";
- ตัวอย่าง 5 (กรองเงื่อนไขข้อความและปี):
  คำถาม: "โครงการที่มีสถานะอนุมัติในปี 2566"
  SQL: SELECT * FROM "ชื่อตาราง" WHERE "สถานะ" LIKE '%อนุมัติ%' AND "ปี" = 2566 LIMIT 20;
- ตัวอย่าง 6 (คำถามนอกขอบเขตข้อมูล):
  คำถาม: "ขอสูตรทำต้มยำกุ้ง" หรือ "สภาพอากาศวันนี้เป็นยังไง"
  คำตอบ: [OUT_OF_SCOPE] คำถามนี้อยู่นอกเหนือขอบเขตข้อมูลที่มีอยู่ในระบบ
- ตัวอย่าง 7 (การเชื่อมโยงหลายตาราง / Multi-Table JOIN):
  คำถาม: "ค้นหารายชื่อลูกค้าที่มียอดสั่งซื้อสินค้า" หรือคำถามข้ามตารางที่มีความสัมพันธ์กัน
  SQL: SELECT c."name", o."total_amount" FROM "customers" c INNER JOIN "orders" o ON c."id" = o."customer_id" LIMIT 20;
- ตัวอย่าง 8 (กรองคำสำคัญเฉพาะเจาะจง และแจกแจงตารางรายการ):
  คำถาม: "สรุปข้อมูลข้าวในเดือนแม่แบบที่ 1" หรือ "สรุปประเภทข้าวทั้งหมดในเดือนแม่แบบที่ 1" หรือ "แสดงรายการข้าว"
  SQL: SELECT * FROM "ชื่อตาราง" WHERE "เดือนแม่แบบ" = 1 AND ("คำอธิบาย" LIKE '%ข้าว%' OR "ประเภท" LIKE '%ข้าว%') LIMIT 50;
- ตัวอย่าง 9 (คำนวณผลรวมมูลค่าเฉพาะเมื่อผู้ใช้ร้องขอคำว่า "รวมมูลค่า" หรือ "ยอดรวม"):
  คำถาม: "รวมมูลค่าข้าวทั้งหมดในเดือนแม่แบบที่ 1" หรือ "ยอดรวมมูลค่าข้าวในเดือนที่ 1"
  SQL: SELECT COUNT(*) AS "จำนวนรายการ", SUM("มูลค่า") AS "รวมมูลค่า" FROM "ชื่อตาราง" WHERE "เดือนแม่แบบ" = 1 AND ("คำอธิบาย" LIKE '%ข้าว%' OR "ประเภท" LIKE '%ข้าว%');
- ตัวอย่าง 10 (แจกแจงประเภทเฉพาะเจาะจงตามมูลค่า):
  คำถาม: "แจกแจงประเภทข้าวในเดือนแม่แบบที่ 1 ตามมูลค่า"
  SQL: SELECT "คำอธิบาย", SUM("มูลค่า") AS "มูลค่า" FROM "ชื่อตาราง" WHERE "เดือนแม่แบบ" = 1 AND ("คำอธิบาย" LIKE '%ข้าว%' OR "ประเภท" LIKE '%ข้าว%') GROUP BY "คำอธิบาย" ORDER BY "มูลค่า" DESC LIMIT 20;

ข้อบังคับอย่างเคร่งครัดในการสร้างคำสั่ง SQL:
1. คืนค่าเฉพาะคำสั่ง SQL เพียวๆ เท่านั้น ห้ามมีคำอธิบาย ห้ามมีข้อความนำหน้า ห้ามใส่ markdown block ครอบ
2. **การเชื่อมโยงหลายตาราง (Multi-Table JOIN)**:
   - หากคำถามถามถึงข้อมูลที่มีความสัมพันธ์ข้ามตาราง (ให้ดูจาก Foreign Keys ใน Schema) ให้ใช้ `INNER JOIN` หรือ `LEFT JOIN` ผ่านคอลัมน์ที่เป็น Foreign Key เพื่อรวมข้อมูลออกมาอย่างถูกต้อง
3. **การเลือกคอลัมน์ให้ครบถ้วน (Comprehensive SELECT)**:
   - หากมีการกรองเงื่อนไข `WHERE` บนคอลัมน์ใด (เช่น `WHERE period_of_inv LIKE '%น้อยกว่า 30 วัน%'` หรือ `WHERE year = 2558`):
     **ต้อง SELECT คอลัมน์ที่กรองนั้น (เช่น `period_of_inv`, `year`) และคอลัมน์ตัวเลข/หน่วย (`value`, `unit`) ออกมาในผลลัพธ์ด้วยเสมอ** เพื่อให้ระบบสรุปรายงานและสร้างกราฟได้อย่างสมบูรณ์
   - หากคอลัมน์เป็นข้อความระบุช่วงเวลา เช่น `period_of_inv`, `period`: ให้ใช้เงื่อนไขแบบข้อความ `WHERE period_of_inv LIKE '%น้อยกว่า 30 วัน%'` (ห้าม CAST เป็นตัวเลข)
4. **การกรองคำสำคัญ / วัตถุ / สินค้าเฉพาะเจาะจง (Keyword & Specific Entity Filtering - สำคัญยิ่ง)**:
   - หากคำถามของผู้ใช้มีคำสำคัญที่ระบุสินค้า, สิ่งของ, กลุ่ม หรือชื่อเฉพาะ (เช่น 'ข้าว', 'ยางพารา', 'น้ำมัน', 'เหล็ก', 'ทองคำ', 'รถยนต์', หรือคำเฉพาะอื่นๆ):
     **ต้องใส่เงื่อนไข `WHERE ("คอลัมน์ชื่อ/คำอธิบาย" LIKE '%คำสำคัญ%' OR "คอลัมน์ประเภท" LIKE '%คำสำคัญ%')` เสมอ!**
     ห้ามละเลยคำสำคัญนั้นแล้วไปดึงทุกรายการในตารางโดยเด็ดขาด เพื่อป้องกันไม่ให้ข้อมูลรายการอื่นที่ไม่เกี่ยวข้องปนเข้ามา
5. **การแจกแจงตารางรายการ (Itemized Rows Breakdown) vs การหาผลรวมมูลค่า (Aggregate SUM)**:
   - เมื่อผู้ใช้ถาม "สรุปข้อมูล...", "สรุปประเภท...", "ข้อมูล...", "รายการ...":
     ผู้ใช้ต้องการดู **"ตารางแจกแจงรายการข้อมูล" (Detailed Rows)** เพื่อตรวจดูรายการย่อย
     ให้ใช้ `SELECT * FROM "ชื่อตาราง" WHERE ... LIKE '%คำสำคัญ%' LIMIT 50;` (ห้ามนำมา GROUP BY หรือ SUM ยุบรวมเป็นยอดเดียว เว้นแต่ผู้ใช้จะสั่งให้รวม)
   - **ใช้ `SUM("มูลค่า")` หรือการคำนวณผลรวมเฉพาะเมื่อผู้ใช้เอ่ยคำว่า "รวมมูลค่า", "ยอดรวม", "ผลรวม", "รวมทั้งหมด" อย่างชัดเจนเท่านั้น!**
6. **การให้ความสำคัญกับตารางที่ผู้ใช้อัปโหลดเข้ามา (User Uploaded Tables Priority - สำคัญสูงสุด)**:
   - หากใน Schema มีส่วน "=== ตารางข้อมูลที่ผู้ใช้อัปโหลดเข้ามา ===":
     **ให้ถือว่าผู้ใช้ต้องการสอบถามและวิเคราะห์ข้อมูลจากตารางที่อัปโหลดเข้ามานี้เป็นหลักเสมอ**
   - เมื่อผู้ใช้ถามคำถามกว้างๆ คำถามง่ายๆ หรือถามภาพรวม เช่น "แสดงข้อมูลทั้งหมดในชุดข้อมูล", "แสดงข้อมูลทั้งหมด", "แสดงรายการข้อมูล", "สรุปข้อมูลในชุดข้อมูล", "แสดงข้อมูล 10 รายการแรก":
     - คำว่า "ชุดข้อมูล", "รายชื่อข้อมูล", หรือ "ข้อมูลที่มี" หมายถึง "รายการข้อมูลในตารางที่อัปโหลด" ไม่ได้หมายถึง "รายชื่อลูกค้า" (ห้ามไป SELECT customers เด็ดขาด!)
     - **ต้องสร้าง SQL ดึงข้อมูลจากตารางที่ผู้ใช้อัปโหลดเข้ามา** เช่น `SELECT * FROM "ชื่อตารางที่อัปโหลด" LIMIT 20;`
   - เมื่อผู้ใช้ถาม "สรุปภาพรวมและสถิติสำคัญของข้อมูล" หรือ "สรุปภาพรวมข้อมูล":
     - หากตารางมีคอลัมน์ตัวเลข (เช่น งบประมาณ, จำนวนเงิน, ยอด, ราคา, มูลค่า): สามารถดึงข้อมูล `SELECT * FROM "ชื่อตาราง" LIMIT 20;` หรือคำนวณ `COUNT(*)`, `SUM(...)`, `AVG(...)` ของคอลัมน์ตัวเลขหลัก
     - หากไม่มีคอลัมน์ตัวเลข ให้ใช้ `SELECT COUNT(*) AS "จำนวนทั้งหมด" FROM "ชื่อตาราง";` หรือ `SELECT * FROM "ชื่อตาราง" LIMIT 20;`
   - เมื่อผู้ใช้ถาม "ค้นหา 5 อันดับแรกที่มีค่ามากที่สุด" หรือ "5 อันดับแรก":
     - ให้หาคอลัมน์ตัวเลขหลักในตาราง (เช่น งบประมาณ, จำนวนเงิน, ยอด, ราคา, คะแนน, จำนวน) แล้วสร้างคำสั่ง `SELECT * FROM "ชื่อตาราง" ORDER BY "คอลัมน์ตัวเลข" DESC LIMIT 5;`
     - หากไม่มีคอลัมน์ตัวเลข ให้ใช้ `LIMIT 5;`
   - **ห้ามไปดึงข้อมูลจากตาราง mock data (customers, products, orders) มาตอบเด็ดขาด** เมื่อมีตารางที่ผู้ใช้อัปโหลดอยู่ในฐานข้อมูล เว้นแต่ผู้ใช้จะเอ่ยคำว่า "ลูกค้า", "คำสั่งซื้อ", "สินค้า" ออกมาอย่างชัดเจน
   - **กรณีไม่มีตารางที่อัปโหลดอยู่ใน Schema (มีเฉพาะ Mock Tables)**:
     - หากผู้ใช้ไม่ได้ระบุชัดเจนว่าต้องการทดสอบด้วยข้อมูลจำลอง (Mock Data) หรือไม่ได้เอ่ยถึง "ลูกค้า", "สินค้า", "คำสั่งซื้อ":
       **ห้ามนำตาราง mock data (customers, products, orders) มาสวมรอยตอบเป็นอันขาด** ให้ตอบว่าไม่พบชุดข้อมูลที่นำเข้าในระบบ
7. หากต้องแสดงข้อมูลที่เกี่ยวข้อง เช่น ชื่อสินค้า หรือ ชื่อลูกค้า ในตาราง mock ให้ใช้ JOIN เพื่อดึง name มาแสดงเสมอ ไม่แสดงเฉพาะ foreign key id
8. ใช้เฉพาะชื่อตารางและชื่อคอลัมน์ที่ระบุใน Schema เท่านั้น หากชื่อตารางมีภาษาไทยให้ครอบด้วยเครื่องหมาย double quotes เสมอ เช่น `FROM "ชื่อตาราง"`
9. คำสั่ง SQL ต้องขึ้นต้นด้วย SELECT หรือ WITH เท่านั้น
10. ไม่ใช้คำสั่ง DDL หรือ DML เช่น DROP, DELETE, INSERT, UPDATE เด็ดขาด
11. **คำสั่งประเภท "แจกแจง", "จัดกลุ่ม", "แบ่งตาม", "ตามแต่ละ..." (Categorical Breakdown / Grouping ยืดหยุ่นทุกประเภทข้อมูล)**:
    - หากผู้ใช้ขอให้ "แจกแจง", "จัดกลุ่มตามหมวดหมู่", "นับจำนวนตามแต่ละ..." หรือ "แบ่งตาม...":
      **ต้องสร้างคำสั่ง SQL ที่ใช้ GROUP BY คอลัมน์หมวดหมู่นั้นๆ พร้อมกับ COUNT(*) เสมอ (ห้ามใช้ SELECT * ทั้งตาราง)**
      เช่น `SELECT "อำเภอ", COUNT(*) AS "จำนวนรายการ" FROM "ชื่อตาราง" GROUP BY "อำเภอ" ORDER BY "จำนวนรายการ" DESC LIMIT 10;`
    - หากผู้ใช้ระบุคำสำคัญหรือสินค้าเฉพาะเจาะจงด้วย (เช่น 'แจกแจงประเภทข้าว...'): **ต้องคงเงื่อนไข WHERE กรองคำสำคัญนั้นไว้เสมอ** เช่น `WHERE ("คำอธิบาย" LIKE '%ข้าว%' OR "ประเภท" LIKE '%ข้าว%') GROUP BY ...`
    - หากผู้ใช้ถามว่า 'สรุปประเภท [สินค้า]...' หรือ 'ประเภท [สินค้า] มีอะไรบ้าง' โดยไม่ได้บอกว่าให้จัดกลุ่มนับจำนวน ให้มองเป็นการกรองรายการที่มี [สินค้า] แล้วแจกแจงตารางรายการ `SELECT * FROM ... WHERE ... LIKE '%สินค้า%' LIMIT 50;`
    - หากผู้ใช้ถามคำถามแบบกว้างๆ เช่น "แจกแจงจำนวนรายการตามแต่ละหมวดหมู่":
      ให้ตรวจสอบคอลัมน์ใน Schema ของตารางนั้น แล้วเลือกคอลัมน์หมวดหมู่ที่เป็นข้อความที่เหมาะสมที่สุดตามลักษณะข้อมูล:
      - **ข้อมูลงบประมาณ/การเงิน**: เลือกคอลัมน์ เช่น "หมวดรายจ่าย", "แผนงาน", "ผลผลิต", "โครงการ", "หน่วยงาน", "ประเภท", "สถานะ"
      - **ข้อมูลสถานที่/ท่องเที่ยว/ร้านอาหาร**: เลือกคอลัมน์ เช่น "จังหวัด", "อำเภอ", "ตำบล", "ประเภท", "กลุ่ม", "หมวดหมู่"
      - **ข้อมูลทั่วไป**: เลือกคอลัมน์หมวดหมู่/กลุ่ม/ประเภท ที่มีค่าหลากหลายและไม่ใช่ ID หรือ วันที่
      สร้างคำสั่ง GROUP BY คอลัมน์นั้น พร้อม COUNT(*) AS "จำนวนรายการ" เรียงจากมากไปน้อย LIMIT 10
12. **การรับมือกับคำถามนอกขอบเขตข้อมูล (Out-of-Scope Guardrails & Zero Hallucination)**:
    - หากคำถามของผู้ใช้อยู่นอกเหนือจากขอบเขตของข้อมูลหรือโครงสร้างตาราง (Database Schema) ที่ระบุไว้ เช่น คำถามทั่วไปในชีวิตประจำวัน ("อากาศวันนี้เป็นยังไง", "ขอสูตรทำผัดกะเพรา", "ช่วยแต่งกลอน", "ใครชนะบอล"):
      - **ห้ามสร้างคำสั่ง SQL และห้ามเดาหรือสมมติตารางที่ไม่มีอยู่จริงใน Schema โดยเด็ดขาด (Zero Hallucination)**
      - ให้ตอบกลับขึ้นต้นด้วย `[OUT_OF_SCOPE]` ตามด้วยคำอธิบายสั้นๆ เช่น:
        `[OUT_OF_SCOPE] คำถามนี้อยู่นอกเหนือขอบเขตข้อมูลที่มีอยู่ในระบบ`
"""
    return prompt
