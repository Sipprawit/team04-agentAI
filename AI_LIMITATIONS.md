# 🤖 ขีดจำกัดและศักยภาพของระบบ AI — Text-to-SQL AI Agent

## 1. โมเดล AI ที่ใช้ในโปรเจค

| รายการ | รายละเอียด |
|--------|-----------|
| **Provider** | Groq (ฟรี API) |
| **โมเดลเริ่มต้น** | `openai/gpt-oss-20b` (ขนาด 20 พันล้านพารามิเตอร์) |
| **ทางเลือกที่ตั้งค่าได้** | `openai/gpt-oss-120b` (120B), `qwen/qwen3.6-27b` (27B) |
| **Temperature** | 0.2 (ความแม่นยำสูง, ความคิดสร้างสรรค์ต่ำ) |
| **Max Tokens** | 2,048 tokens ต่อคำตอบ |
| **ค่าใช้จ่าย** | ฟรี (Groq Free Tier) |

---

## 2. ขีดจำกัดของโมเดลปัจจุบัน

### 2.1 ความแม่นยำในการสร้าง SQL
- **ระดับประมาณ 70-80%** สำหรับคำถามที่ซับซ้อน (JOIN หลายตาราง, Subquery, Window Functions)
- คำถามง่ายๆ เช่น `SELECT * FROM table` หรือ `GROUP BY` ทำได้ดี (~95%)
- คำถามที่ต้องตีความภาษาไทยซับซ้อน อาจเข้าใจผิดหรือสร้าง SQL ที่ไม่ตรงบริบท

### 2.2 ความเข้าใจภาษาไทย
- โมเดลขนาด 20B มีการฝึกภาษาไทยน้อยกว่าโมเดลขนาดใหญ่ (GPT-4, Claude 3.5 ฯลฯ)
- อาจตีความคำถามภาษาไทยบางประโยคผิดได้ เช่น "แจกแจง" อาจถูกตีความว่า "แจก" แทน "จัดกลุ่มแสดง"
- คำศัพท์เฉพาะทาง (งบประมาณ, ไตรมาส, พ.ศ.) อาจต้องใช้ร่วมกับ Prompt Engineering ที่แม่นยำ

### 2.3 การสรุปข้อมูลเชิงวิเคราะห์ (Executive Insight)
- คำตอบอาจมีความลำเอียง (Bias) ไปทางข้อมูลธุรกิจ/ยอดขาย เนื่องจากข้อมูลฝึกสอนส่วนใหญ่เป็นภาษาอังกฤษเชิงธุรกิจ
- ระบบมี Guardrails (กฎควบคุม) ที่สั่งไม่ให้สมมติบริบท แต่โมเดลขนาดเล็กอาจไม่ปฏิบัติตามได้ 100%
- ตัวเลขอาจถูก Round หรือหายไปในบางครั้ง → ระบบมี Post-processing เพิ่มเครื่องหมายลูกน้ำให้

### 2.4 ข้อจำกัดเชิงเทคนิค (Groq Free Tier)
- **Rate Limit**: จำกัดจำนวนคำขอต่อนาที (ขึ้นอยู่กับ Groq Free Tier ณ ขณะนั้น)
- **Context Window**: จำกัดขนาดข้อมูลที่ส่งให้ AI ได้ต่อครั้ง (ประมาณ 8K-32K tokens)
- **ไม่สามารถ Fine-tune** ได้ใน Free Tier

---

## 3. สิ่งที่ระบบทำได้ดีอยู่แล้ว

| ✅ ความสามารถ | รายละเอียด |
|--------------|-----------|
| **แปลงคำถามเป็น SQL** | รองรับ SELECT, JOIN, GROUP BY, ORDER BY, WHERE, LIKE |
| **ป้องกันความปลอดภัย** | บล็อก DROP, DELETE, INSERT, UPDATE ทั้งหมด |
| **Self-healing SQL** | เมื่อ SQL ผิด ระบบแก้ไขตัวเองอัตโนมัติสูงสุด 2 ครั้ง |
| **Out-of-Scope Guardrails** | ปฏิเสธคำถามนอกขอบเขตอย่างสุภาพ |
| **กราฟอัจฉริยะ** | Auto-EDA แนะนำประเภทกราฟที่เหมาะสม (Bar, Line, Pie, Area) |
| **รองรับ CSV อัปโหลด** | วิเคราะห์ข้อมูลจากไฟล์ CSV ที่ผู้ใช้นำเข้า |

---

## 4. แนวทางพัฒนาต่อยอดเพื่อเพิ่มศักยภาพ

### ระดับ 1: ปรับปรุงได้ทันที (ไม่มีค่าใช้จ่าย)
- ✅ ปรับปรุง Prompt Engineering ให้แม่นยำขึ้น (ทำแล้ว)
- ✅ เพิ่ม Guardrails ป้องกัน Bias (ทำแล้ว)
- ✅ Post-processing ตัวเลขและข้อความ (ทำแล้ว)
- 🔄 เปลี่ยนโมเดลเป็น `openai/gpt-oss-120b` (120B params) — แม่นยำขึ้น แต่ช้าลง

### ระดับ 2: ต้องลงทุนเพิ่ม (ค่าใช้จ่ายปานกลาง)
- 💰 อัปเกรดเป็น Groq Pro/Developer Tier → Rate Limit สูงขึ้น
- 💰 ใช้โมเดลเชิงพาณิชย์ เช่น GPT-4o, Claude 3.5 Sonnet ผ่าน API
- 💰 เพิ่ม RAG (Retrieval-Augmented Generation) เพื่อให้ AI มีบริบทข้อมูลมากขึ้น

### ระดับ 3: พัฒนาเชิงลึก (ระยะยาว)
- 🔬 Fine-tune โมเดลด้วย Dataset ภาษาไทย + SQL
- 🔬 สร้าง Few-shot Examples Library สำหรับแต่ละประเภทคำถาม
- 🔬 เพิ่ม Evaluation Pipeline ทดสอบความแม่นยำอัตโนมัติ
- 🔬 รองรับ Multi-modal (รูปภาพ, PDF, Excel)

---

## 5. สรุป

> โปรเจคนี้ใช้โมเดล AI ฟรี (Groq Free Tier, 20B params) ซึ่งมีข้อจำกัดด้านความแม่นยำ
> โดยเฉพาะคำถามซับซ้อนและภาษาไทยเชิงลึก อย่างไรก็ตาม ระบบมีกลไกป้องกันหลายชั้น
> (Security Validator, Self-healing, Out-of-Scope Guardrails, Post-processing)
> ที่ช่วยลดข้อผิดพลาดและเพิ่มความน่าเชื่อถือ
>
> หากต้องการความแม่นยำที่สูงขึ้น สามารถอัปเกรดโมเดลเป็น `gpt-oss-120b`
> หรือใช้บริการ AI เชิงพาณิชย์ (GPT-4o, Claude) ได้ในอนาคต
